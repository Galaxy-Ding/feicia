# 阶段 04 问题点

## 1. 当前仍未解决的问题

- session history 目前是窗口拼接，还没有摘要压缩机制。
- `prompt` 阶段虽然使用 reference-map，但 validator 仍未利用全部真实资产元数据做强校验。
- API Key 明文回退问题仍未解决。
- 黄金样例验收和最终验收证据仍未执行。

## 2. 建议处理方案

- Phase 5/6 应优先做黄金样例和最终验收证据收集。
- 对会话历史，后续应补 summary 压缩，避免上下文无限增长。
- 对安全项，应把明文 API Key 回退改成显式开发模式开关。

## 3. 可能需要用户决策的点

- 是否允许我继续直接处理 API Key 策略，默认禁止明文 key。
- 是否有可作为黄金样例的真实 episode，供最终验收使用。
