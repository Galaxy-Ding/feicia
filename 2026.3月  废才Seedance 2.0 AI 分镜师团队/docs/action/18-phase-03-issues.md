# 第三阶段问题点

## 已解决

1. 已解决：修改请求回流缺少命令入口。
2. 已解决：状态检测只判断资产文件存在，不能判断当前集是否已完成服化道。
3. 已解决：跨集资产缺少按集块级管理。

## 保留项

1. 保留：分镜素材映射的“场景独立编号”目前主要依赖模型遵守 Prompt 规则，后续可以增加代码级校验器。
2. 保留：当前未实现 GUI。
3. 保留：当前未接数据库与多用户。
