# 阶段 4 总结

## 上一阶段回顾

阶段 3 已完成页面与浏览器抽象，下一步重点是把下载、归档和命名真正打通。

## 阶段目标

完成下载目录监听、文件稳定判断、重命名和映射记录。

## 本阶段完成

- 实现 `wait_for_stable_file()`
- 实现 `archive_downloads()`
- 原始下载保留在 `downloads/staging/`
- 归档文件输出到 `downloads/images/`
- 追加写入映射 JSONL

## 结论

阶段 4 已完成本地下载与命名闭环。
