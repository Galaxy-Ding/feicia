# 阶段02-问题点

## 1. 高优先级

- `HanLP` 与 `BookNLP` 当前仍是 fallback contract，必须在下一阶段接真实引擎，否则抽取质量没有正式基线
- alias merge 和 review queue 还未开始，`CAS-002` 仍是系统主风险
- `video_only_once_phase1` 尚未接管 `character_action` 命令与 manifest，统一调度仍未闭环

## 2. 中优先级

- 需要补多本中英文样书，不能只依赖单一 demo
- 需要补 SQLite 到 Postgres 的迁移脚本与字段对齐策略
- 需要把 schema 校验从“文件基线”升级到“运行时强校验”

## 3. 低优先级

- prompts/zh 与 prompts/en 目录目前只是占位，后续可以随真实抽取器一起完善
- API 导出能力可以在 review queue 形成之后再补

## 4. 处理原则

- 先把真实预处理器接入，再进入 alias merge 与 relation build
- 新阶段开始前，必须先回顾本文件与 `阶段02-阶段总结.md`
- 新增问题继续同步更新 `07-issue-log.md`
