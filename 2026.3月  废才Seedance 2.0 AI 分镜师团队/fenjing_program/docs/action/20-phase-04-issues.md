# 第四阶段问题点：真实剧本联调

## 已解决

1. 已解决：真实剧本已经完成三阶段真实生成。
2. 已解决：真实 API 与当前工程配置已经打通。
3. 已解决：`responses` 接口兼容已验证可用。

## 新发现问题

1. 自动审核器在真实长文本场景下过严，容易连续 FAIL。
2. 当前工作流缺少“人工放行”机制，导致生成成功但流程被审核阻断。
3. 网络超时在真实长响应场景下偏紧，已从 120 秒调整到 300 秒，但后续仍建议做可配置化优化。

## 建议优先级

1. P0：增加人工验收/人工放行命令
2. P1：增加审核器与生成器的一致性调优
3. P1：记录每轮失败的完整审核报告到文件，便于离线修订
