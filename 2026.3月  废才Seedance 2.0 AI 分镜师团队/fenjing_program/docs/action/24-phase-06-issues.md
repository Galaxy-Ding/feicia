# 第六阶段问题点

## 已解决

1. 已解决：人工放行前缺少统一决策材料，只能在 `summary.json` 和两份轮次报告之间手工切换。
2. 已解决：`accept` 虽能放行，但没有只读审核入口，人工无法先稳定预览再决策。
3. 已解决：仓库根目录直接运行测试时存在包导入路径问题，不够“拿来即跑”。
4. 已解决：`08-issue-log.md` 存在重复的 `I-013` 编号。

## 保留项

1. 自动审核对真实长文本仍偏严格，`ep01` 仍依赖人工放行闭环，后续需要优化审核器阈值和回修提示。
2. 当前亮点提取使用规则抽取，已经够用，但后续可升级为更细的结构化优点/缺点抽取。
3. `overview.md` 当前偏工程管理视图，后续可以加“是否建议整体放行本集”的聚合判断。
